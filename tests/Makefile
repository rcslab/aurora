SUBDIR = array compute delta fd fifo fork forkwait journal kqueue llist main memsnap \
	 metrodelta metropolis mmap multithread register pipe pgroup posixshm \
	 print metroclient metroserver metrosimple metroparts sharemap selfie signal sleep \
	 slsfs socketpair sysvshm tcplisten udplisten unixlisten unlink wal walfd

BINDIR=/tmp
.MAKE.EXPORTED=BINDIR

.include <bsd.subdir.mk>
