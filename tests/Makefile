SUBDIR = array compute fd fifo fork forkwait kqueue llist main memsnap \
	 metropolis mmap multithread register pipe posix posixshm \
	 print pymetro pymetro-partition sharemap selfie signal sleep \
	 slsfs socketpair sysvshm tcplisten udplisten unixlisten vmobject \
 	 vmregion unlink wal

.include <bsd.subdir.mk>

.include <bsd.prog.mk>
