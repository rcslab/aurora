SUBDIR = array compute fd fifo fork llist memory mmap \
	 multithread register pipes posixshm print sharemap \
	signal sleep socketpair sysvshm tcplisten udplisten \
	unixlisten unlink

.include <bsd.subdir.mk>
