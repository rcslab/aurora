SUBDIR = array compute fd fifo fork forkwait llist main mmap \
	 multithread register pipe posix posixshm print sharemap \
	signal sleep slsfs socketpair sysvshm tcplisten udplisten \
	unixlisten vmobject unlink

.include <bsd.subdir.mk>

.include <bsd.prog.mk>
